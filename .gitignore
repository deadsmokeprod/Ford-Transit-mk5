# --- Системные / OS ---
Thumbs.db
ehthumbs.db
Desktop.ini
.DS_Store

# --- Большие исходные дампы (не нужны в репозитории) ---
# Сохранённая страница каталога Bosch на 4.5 МБ — пусть лежит локально, но в git её не тащим
*.mhtml

# --- Cursor / IDE служебное ---
terminals/
agent-tools/
agent-transcripts/
.cursor/

# --- Временные / личные заметки ---
*.tmp
*.bak
*.swp
*~

# --- Архивные бэкапы ---
backup/
*.zip
